# 参与开发

先读 README 和 docs/public/RUNNING.md。推荐从可复现缺陷、供应商能力研究、字幕/媒体兼容性和文档入手。

## 本地检查

```bash
npm ci
npm test
npm run demo
```

涉及媒体的修改另运行 fixture:media；涉及 DSH 的修改运行固定版本 smoke。说明实际运行环境与未验证项。

## 约定

- 契约当前为 0.2.1。改变 schema、依赖失效规则或费用语义时，在 PR 里说明迁移和反例。
- 模型提出结构化计划，工具校验后执行；不执行模型拼接的任意 shell。
- 不自动上传真实素材或调用收费服务。新增真实供应商需有显式授权、额度与素材范围设计。
- 未知提交保持预留，不通过更换 operation ID 重发。
- fixture/mock/live/external_manual 分开记录；技术通过不等于内容质量通过。
- 测试应覆盖有意义的失败与恢复行为，不把实现重复一遍当验证。
- 不提交 API Key、个人路径、运行账本、第三方视频、字体或模型权重。

Issue 请提供版本、系统、复现命令、预期与实际结果，删除私密内容。PR 描述解释问题、修改后的行为、验证和剩余限制。
